
import { Suspense } from "react";
import VerifyContent from "@/components/forgot-password/VerifyContent";

export default function Page() {
  return (
    <Suspense>
      <VerifyContent />
    </Suspense>
  );
}

